using DataProject;

Console.WriteLine("Hello, Data Project . . .");

// Can't call ProcessData() because we don't have class implementaions for the respective interfaces.
//IRepository repository;
//ILogger logger;
//DataService dataService = new DataService(repository, logger);
//dataService.ProcessData(1);

Console.ReadKey();